# Vango V32F20x GPIO configuration options

config GPIO_V32F20X
    bool "Vango V32F20x GPIO driver"
    default y
    depends on DT_HAS_VANGO_V32F20X_GPIO_ENABLED
    select GPIO
    select VANGO_V32F20X_HAL_GPIO
    help
      Enable the Vango V32F20x GPIO driver.
