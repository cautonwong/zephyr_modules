description: V85XXP DMA controller

compatible: "v85xxp,dma"

include:
  - name: dma-controller.yaml
  - name: base.yaml

properties:
  reg:
    required: true

  interrupts:
    required: true

  "#dma-cells":
    const: 1

  dma-channels:
    required: true

  dma-requests:
    required: true
